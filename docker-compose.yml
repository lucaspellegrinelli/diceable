services:
  redis:
    image: redis
    container_name: redis
    command: redis-server --requirepass password
    ports:
      - 6379:6379

  ws-proxy:
    build:
      context: ./proxy
      dockerfile: Dockerfile
    container_name: ws-proxy
    ports:
      - 5555:5555
    environment:
      - PORT=5555
      - ORIGIN_SOURCE=localhost:3000
      - LOKI_URL=
      - LOKI_USERNAME=
      - LOKI_PASSWORD=

  bot:
    build:
      context: ./discordbot
      dockerfile: Dockerfile.dev
    container_name: bot
    depends_on:
      - ws-proxy
      - redis
    environment:
      - DISCORD_TOKEN=
      - LOKI_URL=
      - LOKI_USERNAME=
      - LOKI_PASSWORD=
      - REDISHOST=redis
      - REDISPASSWORD=password
      - REDISPORT=6379
      - REDISUSER=default
      - WEBSOCKET_HOST=ws-proxy:5555
      - S3_ACCESS_KEY_ID=
      - S3_BUCKET=
      - S3_ENDPOINT=
      - S3_REGION=
      - S3_SECRET_ACCESS_KEY=
    volumes:
      - ./discordbot:/app
      - ./discordbot/data:/app/data

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile.dev
    container_name: frontend
    depends_on:
      - redis
    ports:
      - 3000:3000
    environment:
      - AUTH_SECRET=secret
      - AUTH_TRUST_HOST=true
      - DISCORD_CLIENT_ID=
      - DISCORD_CLIENT_SECRET=
      - LOKI_URL=
      - LOKI_USERNAME=
      - LOKI_PASSWORD=
      - PUBLIC_WEBSOCKET_PROXY=localhost:5555
      - REDISHOST=redis
      - REDISPASSWORD=password
      - REDISPORT=6379
      - REDISUSER=default
      - S3_ACCESS_KEY_ID=
      - S3_BUCKET=
      - S3_ENDPOINT=
      - S3_REGION=
      - S3_SECRET_ACCESS_KEY=
    volumes:
      - ./frontend:/app
      - /app/node_modules

networks:
  local:
    driver: bridge
